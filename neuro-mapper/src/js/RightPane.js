import { useEffect, useRef } from "react";
import * as constant from "./constant.js";
import { observer } from "mobx-react";

export const RightPane = observer(
    ({
      store
    }) => { 
        return (
        <div id="right-pane-wrapper">
        </div>
        )
    }
)